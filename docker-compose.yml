services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: qia
      POSTGRES_PASSWORD: qia
      POSTGRES_DB: qia
    ports:
      - "5434:5432"
    volumes:
      - qia_pgdata:/var/lib/postgresql/data
volumes:
  qia_pgdata:
